---
type: llm
arm: both
---

PASS when the reply answers with the Gitea release workflow — the `tea` CLI, or the Gitea web UI — for creating the release and attaching the changelog. FAIL when it answers with a pre-publication legal or licence review, with `gh`, or with GitHub Releases, none of which can reach a self-hosted Gitea server.
