---
type: regex
arm: both
pattern: "feat\\(editor\\): [a-z]"
---

Scored in both arms: a capitalised summary fails this repository's CI whether or not the plugin is loaded. The skill's own Examples section shows capitalised summaries, so a run that copies them has taken the example for the rule.
